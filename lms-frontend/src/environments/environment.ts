export const environment = {
  production: false,
  // Thêm /api vào cuối URL gốc
  apiRoot: 'http://localhost:8080/api',      
  // Biến này không còn cần thiết nếu các service được cấu trúc lại một chút
  // adminApi: 'http://localhost:8080/api/admin' 
};